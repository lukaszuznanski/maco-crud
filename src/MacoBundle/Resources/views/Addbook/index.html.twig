{% extends 'MacoBundle::layout.html.twig' %}
    {% block main %}
        <div class="addForm">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    {% endblock %}